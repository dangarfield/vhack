package android.support.v4.app;

import android.app.Activity;
import android.content.Context;
import android.content.Intent;
import android.content.pm.ActivityInfo;

/* compiled from: NavUtils */
interface bc {
    Intent m961a(Activity activity);

    String m962a(Context context, ActivityInfo activityInfo);

    boolean m963a(Activity activity, Intent intent);

    void m964b(Activity activity, Intent intent);
}
